// =============================================================================
// Module Configuration File - Required
// =============================================================================

module.exports = {
  version: "3.0.0",
  description: `Legace fluid grid system. Uses percentages for gutters. 
                Uses downward breakpoints and can adapt column sizes on 
                each breakpoints.`,
  require: ["grid"]
};