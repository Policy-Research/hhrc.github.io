const LessFrameworkCompiler = require('../index.js').compiler;

const options = {
  pathSite: '../../../../site',
  pathCore: '../../../../core',
  pathJavascript: "javascript/less-includes.js"
};
const compilerOptions = {
  outputPath: './output/styles.css',
};

const lessFrameworkCompiler = new LessFrameworkCompiler(compilerOptions, null, options);

exports.instance = lessFrameworkCompiler;