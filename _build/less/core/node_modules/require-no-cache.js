// Simple module that clears the cache before requiring
// User passes their require function this passes a 
// function back to use

module.exports = function(requirePassed) {
  return function(filePath){
    delete require.cache[requirePassed.resolve(filePath)];
    return requirePassed(filePath);
  };
};